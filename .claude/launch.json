{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "web",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["ng", "serve", "--port", "4200", "--no-open"],
      "port": 4200,
      "cwd": "${workspaceFolder}/web"
    }
  ]
}
